service cloud.firestore {
  match /databases/{database}/documents {
    // Tutti possono leggere i dati
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
