(function (window) {
  window["env"] = window["env"] || {};

  // Imposta la variabile di ambiente per l'URL dell'API
  window["env"]["apiUrl"] = "http://localhost:3000";

  // Altre variabili di ambiente
})(this);
